const config = {
    // eslint-disable-next-line no-undef
    IdentityServerUri: globalVars.IdentityServerUri,
    // eslint-disable-next-line no-undef
    GameServiceUri: globalVars.GameServiceUri,
};

export default config;